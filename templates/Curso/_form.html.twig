{{ form_start(form) }}

<div class="d-flex justify-content-center">
    <div class="row w-50">
        <div class="col-sm-12">
            {{ form_label(form.courseName) }}
            {{ form_widget(form.courseName) }}
        </div>
        <div class="col-sm-6">
            {{ form_label(form.workload) }}
            {{ form_widget(form.workload) }}
        </div>
        <div class="col-sm-6">
            {{ form_label(form.modality) }}
            {{ form_widget(form.modality) }}
        </div>
        <div class="col-sm-12 mt-3">
            {{ form_label(form.alunos) }}
            {{ form_widget(form.alunos) }}
        </div>
        {{ form_widget(form) }} <br>
    </div>
</div>

<div class="d-flex justify-content-center">
    <button class="btn btn-primary bnt-rounded me-2" type="submit"> <i class="fa-solid fa-check"></i>
        Salvar</button>
    <a class="btn btn-outline-secondary bnt-rounded" href="{{ path('cursos_index')}}"><i class="fa-solid fa-x"></i>
        Cancelar</a>
</div>
{{ form_end(form) }}